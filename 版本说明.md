# 版本说明

## V1.0 — 基础单组分反应器计算版

V1.0 建立了“选择反应器—选择任务—输入条件—查看结果”的基本流程。

- 支持 BR、CSTR、PFR、PBR 四类理想反应器。
- 支持等温、单一不可逆幂律反应，以及液相恒密度和气相变容计算。
- 可由目标转化率求反应时间、体积或催化剂质量，也可由已知尺度反求出口转化率。
- 提供 CSTR/PFR 对比、PBR 简化压力降、结果曲线、计算依据、CSV 和打印摘要。

V1.0 的边界：反应式固定为 `A → Products`；动力学和转化率以 A 为基准；进料不能表达共反应物、初始产物或惰性物质。

## V1.1 — 多组分化学计量与进料版

V1.1 保留 V1.0 的反应器与求解任务，并扩展反应、动力学和进料模型。

- 可选择 1–4 种反应物和 1–4 种产物。组分从 A 开始连续编号，惰性物质记为 I。
- 计量系数按反应式顺序以小写 `a、b、c、d…` 标识；全部系数有效后才自动生成反应式，例如 `2A+B → 2C`。
- 默认按基元反应处理：每个反应物级数初值等于其计量系数；用户每次修改某一反应物计量系数时，对应级数同步更新。非基元反应仍可随后手动覆盖级数。
- 进料区支持所有反应物、所有产物和惰性物质 I 的入口浓度。
- 限制反应物不再固定为 A，而是由各反应物入口浓度与计量系数的比值自动判定；在反应物进料输入完成后立即显示。若各反应物入口浓度相等，界面会明确说明“等摩尔进料”，再给出限制组分结论；比值并列时显示共同限制反应物。
- 转化率、组分浓度、气相膨胀因子和 BR/CSTR/PFR/PBR 设计方程均改为以自动判定的限制反应物 L 为基准。
- 气相膨胀因子由计量数与全部入口组成自动计算，产物和惰性进料参与总摩尔数与稀释计算。
- 删除界面中的“总级数”展示；各反应物级数仍直接进入多组分幂律速率式。
- 加粗并加深 Part A、B、C 等输入分区的分隔线，色调沿用原有紫色体系。

V1.1 当时仍不包含：可逆反应、多重反应网络、非等温能量衡算、RTD、传质/扩散控制和完整工程物性包。

## V1.2 — 稳态变温与气相 PBR 变压版

V1.2 在 V1.1 多组分模型上增加稳态热量衡算，并修正气相动力学基准。

- 条件输入改为三级结构：先选液相/气相，再选恒温/非恒温；气相 PBR 继续选择恒压/变压。
- 非恒温 PFR/PBR 支持逐组分定压热容、环境温度、总传热系数、比换热面积、反应焓；PBR 还输入床层堆积密度。
- 总传热系数 U 默认 0，表示与外界绝热；反应热容变化由全部组分和计量系数计算，不默认等于 0。
- 反应焓使用 `ΔH_R(T)=ΔH_ref+ΔC_P(T-T_ref)` 随温度修正，Arrhenius 速率常数也使用沿程温度。
- 气相反应统一以各组分分压为输入、动力学和结果基准；液相继续采用浓度基准。
- 气相 PBR 可联立简化 Ergun 压降、物料衡算与能量衡算；压降式包含总摩尔流率与温度修正。
- PFR/PBR 的耦合常微分方程采用四阶 Runge–Kutta 法推进，结果区输出真实转化率、温度和压力曲线。
- 二级条件新增不可逆/可逆选择；可逆反应输入与反应商同单位的参考平衡常数，逆向驱动力由 `Q/K(T)` 自动确定，不重复输入逆反应动力学。
- 平衡常数按含 `ΔC_P` 的 van't Hoff 积分随温度修正，并计算沿程平衡转化率；达到平衡边界时停止正向积分。
- PFR/PBR 新增全部组分摩尔流率分布；非恒温图同时显示反应物流和恒定环境温度；PBR 分布横轴由 W/ρ_b 换算为床层体积。
- 气相 PBR 变压图改为总压力 P（bar）随床层体积变化，而非只显示无量纲压力比。
- 所有结果图支持点击实际计算数据点，显示曲线标签和该点的横、纵坐标；点击空白处或按 Esc 可清除数据提示。
- 所有转化率图的纵轴统一限制为 0–1。可逆反应额外绘制 `T₀−100 K` 至 `T₀+100 K` 范围内的平衡转化率—温度关系。

当前仍不包含：多反应网络、温度相关热容、相变/真实气体、并流/逆流冷却剂温度方程、完整局部 Ergun 物性模型、传质扩散和非等温 CSTR 多重稳态。

## V1.3 — 多反应体系版

V1.3 依据 Chapter 8 将求解状态从“单一转化率决定全部组成”扩展为“每个物种的摩尔数或摩尔流率联立”。

- 新增单一反应/多反应一级选择；多反应首批提供 `A→B、A→C` 平行网络与 `A→B→C` 串联网络。
- 每条反应分别输入参考速率常数、反应级数、活化能；非恒温时分别输入反应焓。
- 使用计量矩阵和 `R_j=Σν_mj r_m` 计算每个物种的净生成速率，不再由单一 X 重建 B、C 组成。
- BR、PFR、PBR 联立多物种 ODE；CSTR 联立全部出口物种的非线性代数方程。
- 液相继续采用浓度动力学，理想气相继续采用分压动力学；气相 PBR 可同时考虑总摩尔流率变化、温度和压降。
- 输出 B/C 总选择性、B 总收率、两条反应速率和物种净速率；平行反应显示瞬时选择性分布。
- 串联反应自动搜索目标中间产物 B 的峰值及对应时间、体积或催化剂质量。
- 多反应非恒温 PFR/PBR 的热源改为 `−ΣΔH_m(T)r_m`，而不是单一反应热项。
- CSV 新增逐反应速率、A/B/C 净生成速率和瞬时选择性列。

V1.3 当前边界：多反应网络限定为上述两类不可逆、1:1 教学网络；暂不支持任意计量矩阵、多反应可逆平衡、半间歇/膜进料和多反应非等温 CSTR 多稳态。

## V1.4 — 通用多反应网络版

V1.4 将 V1.3 的固定网络升级为数据驱动的通用反应网络，同时保留平行与串联快捷模板。

- 自定义网络支持 2–6 个物种和 1–5 条反应；每行带符号计量数自动生成反应式。
- 各反应独立输入正向逐物种级数、参考速率常数、活化能、反应焓和可逆性。
- 可逆步骤按自身的 `Q_m/K_m(T)` 计算净驱动力；`r_m<0` 明确表示净逆向反应。
- 反应焓、热容差和平衡常数均按计量矩阵逐反应计算并使用共同参考温度。
- 用户可指定目标组分和选择性参照组分；总选择性按两者净生成量之比计算，目标收率以入口 A 为基准。
- BR/PFR/PBR 的 ODE 与 CSTR 代数方程不再固定为三物种，而是按当前网络维数动态建立。
- 结果区动态显示全部组分物流、全部反应净速率、全部物种净生成速率、瞬时净选择性和目标组分峰值。
- CSV 列随当前物种数和反应数动态生成。
- 增加计量行完整性、重复反应、矩阵维数、目标/参照组分、可逆平衡常数和 A 转化率基准检查。
- 自动迁移 V1.3 本地缓存中的平行/串联网络、两条反应级数和 A/B/C 进料。

V1.4 当前边界：A 固定作为转化率和流量基准；尚未输入分子式，因而不能自动检查元素守恒；暂不包含符号化 PSSH、刚性 ODE 积分器、半间歇/膜进料和非等温多稳态 CSTR。

## V1.5 — 可信计算基础版

V1.5 优先消除 V1.4 通用网络的三个基础限制，不增加新的反应器类型。

- 转化率观察组分可以从全部网络物种中选择；该物种同时作为入口摩尔流率换算与收率分母基准。
- 多反应目标搜索不再假定整条转化率曲线单调，而是返回第一次达到目标的位置。
- 可输入各物种分子式和电荷，自动检查每条计量反应的元素与电荷守恒；拟组分可明确跳过。
- 多反应 BR/PFR/PBR 使用自适应 Dormand–Prince RK45，并提供隐式刚性积分及自动切换。
- 结果区显示实际求解方法和接受/拒绝步数，非单调转化率与未启用守恒检查均产生提示。
- V1.4 本地缓存自动补齐观察组分、守恒模式、分子式和求解器默认值。

V1.5 当前边界：多反应非等温 CSTR、半间歇、膜反应器和符号化 PSSH 尚未实现。

## V1.6 — 案例工作流版

V1.6 将条件输入重构为“组分—反应集合—入口物流—反应器—计算规格”对象，不改变 V1.5 的核心设计方程。

- 主页面改为流程概览和五张摘要卡；详细条件通过小弹窗编辑，保存后才提交。
- 取消单反应/多反应分支、串联/并联模板入口及预设反应数量；所有案例统一为可逐条添加、编辑和删除的反应集合。
- 组分继续使用 A、B、C 与 I 代号，可选映射实际名称、分子式和电荷；全体系强制采用统一相态，气相默认理想气体。
- 新增固定的 `FEED → R-101 → PRODUCT` 物流概览，为未来反应器自由组合预留对象和连接语义。
- 每条反应独立输入参考速率常数、参考温度、活化能、可逆性、平衡常数和参考反应焓；基元反应自动生成级数。
- 反应器弹窗按 BR/CSTR/PFR/PBR 和相态动态显示温度、换热、催化剂及压降参数。
- 求解器、相对/绝对容差和附加选择性定义移动到“高级选项”。
- V1.5 单反应和多反应本地缓存自动迁移到统一案例，保留原有计算含义。
- “新建计算”直接进入组分—反应—物流—反应器—规格工作流；反应器和题型不再作为前置向导。
- 五类案例对象分别显示就绪状态；输入不完整时给出归属明确的错误并禁用运行，组分删除会保护反应与计算规格依赖。
- 入口状态升级为统一 `MaterialStream`：同时保存各组分/总摩尔流率、摩尔分率、动力学活度、温压和体积流量；求解入口统一从该对象换算。
- 案例支持本地自动保存、V1.6 JSON 导入/导出和创建副本。

V1.6 当前仍为单反应器流程，不支持流程画布自由连接、分流、合流和循环物流。
