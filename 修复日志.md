# 修复日志

## V1.2 — 转化率图纵轴物理范围修复（2026-08-18）

### 问题表现

- 转化率曲线沿用通用图表的自动缩放，纵轴上限会随当前出口点变化，不同工况之间难以直接比较，也可能出现超出转化率物理范围的刻度。

### Bug 原因

- 原图表组件只依据当前数据最小值和最大值推导坐标域，没有为转化率这一有明确物理边界的无量纲量提供专用坐标域。

### 解决方式

- 图表组件新增显式 `xDomain`、`yDomain` 能力；默认转化率曲线、实际/平衡转化率沿程图、平衡转化率—温度图和压降对比转化率图均固定使用 `0≤X≤1`。
- 温度、压力、摩尔流率和 Levenspiel 图继续按各自数据范围缩放，避免把转化率专用规则错误套用到其他物理量。
- 增加回归检查，逐一核对所有转化率图均显式声明 0–1 纵轴。

### 验证

- 自动化测试检查全部转化率图的固定物理域，以及非转化率图的独立缩放逻辑。
- 本地浏览器实测纵轴刻度稳定显示为 0、0.25、0.5、0.75、1。

## V1 — 计算条件页公式渲染修复（2026-08-14）

### 问题表现

- 反应式中的 `A`、`\rightarrow`、`\mathrm{Products}` 被拆行并显示为普通字母，箭头和正体产物名没有正确渲染。
- `k_ref`、`T_ref`、`C_A0`、`X_target` 等字段符号由普通斜体文本显示，下划线没有形成数学下标。

### Bug 原因

1. `MathFormula` 的 JSX 字符串属性写成了 `latex="A\\rightarrow\\mathrm{Products}"`。JSX 引号属性会保留反斜杠，传入 KaTeX 的实际内容含有双反斜杠；KaTeX 将其解释为换行命令，后面的 `rightarrow` 和 `mathrm` 因而变成普通数学字母。
2. 输入字段的 `symbol` 虽然使用了 LaTeX 风格命名，但 `Field` 组件通过普通 `<i>` 元素直接输出字符串，没有经过 KaTeX。
3. 原实现没有针对 JSX 转义差异和字段符号渲染方式的回归测试。

### 解决方式

- 反应式改用 `String.raw` 提供 LaTeX 源串，避免 JSX 属性与 JavaScript 字符串的反斜杠规则混用。
- `Field` 组件统一通过 `MathFormula` 渲染所有字段符号，删除冗余的普通斜体符号实现及对应 CSS。
- 将 `k_ref`、`T_ref` 规范为 `k_{\mathrm{ref}}`、`T_{\mathrm{ref}}`，确保下标成组且 `ref` 使用正体；其余下标符号也由同一组件正确处理。
- 新增公式渲染回归测试，检查反应箭头、`\mathrm`、数学下标，以及禁止再次在 JSX LaTeX 字面量中写入双反斜杠命令。

### 验证

- `npm test`：覆盖公式命令、字段下标和源码防回归规则。
- `npm run build`：执行 TypeScript 检查并生成生产构建。
- 本地浏览器实测：反应式显示为 `A → Products`；`k_ref`、`T_ref`、`C_A0`、`F_A0`、`X_target` 均生成数学下标；控制台无错误。

## V1 — 结果数字比例与排布修复（2026-08-14）

### 问题表现

- 主结果数值明显大于其余结果，卡片宽度和高度也不相等，同一层级的数据缺少统一的视觉尺度。
- 主结果跨两行，其他结果被挤进较窄的两列；结果项目数量变化时，布局更容易出现不平衡。

### Bug 原因

1. 结果网格将主列固定为其他列的 `1.4` 倍，并让主卡跨两行。
2. 主结果字号硬编码为 `44px`，其他结果为 `28px`，人为制造了过大的字号差。
3. 仅主卡显示“计算状态：有效”，这条重复信息额外撑高了卡片。

### 解决方式

- 结果区改为 `auto-fit + minmax` 的等宽响应式网格，根据可用宽度自动排列卡片。
- 所有结果数字统一为 `36px`，启用等宽数字特性，使小数位纵向和横向排列更稳定。
- 所有卡片使用同一最小高度、内边距、标题行和单位对齐规则；主结果仅保留紫色强调，不再改变尺寸。
- 删除冗余的“计算状态：有效”及其专属样式。
- 新增结果卡布局回归测试，防止主卡专属字号或跨行规则再次出现。

### 验证

- `npm test`：覆盖等宽网格、统一数字字号、禁止主卡跨行及冗余状态文案。
- `npm run build`：执行 TypeScript 检查并生成生产构建。
- 本地浏览器复测 BR 结果：4 张卡片均为 `277 × 140px`，数值字号均为 `36px`，数值与单位底部基线一致，控制台无错误。

## V1.0 — 积分上下限排版修复（2026-08-14）

### 问题表现

- 结果依据中的 BR、PFR、PBR 设计方程虽然使用展示公式，但积分上下限仍偏在积分号右侧，视觉上不居中。

### Bug 原因

- LaTeX 使用了普通 `\\int_0^X`。KaTeX 会依当前数学样式决定上下限位置，现有写法没有明确要求上下限位于积分号正上方和正下方。

### 解决方式

- BR、PFR、PBR 的设计方程统一改为 `\\displaystyle` 与 `\\int\\limits_{0}^{X}`。
- 公式速查页与实际计算依据使用同一排版规则，避免两处表现不一致。
- 增加回归检查，确保设计积分继续使用显式 `limits`。

### 验证

- KaTeX 输出中积分节点包含上下限结构；本地页面目视检查上下限已位于积分号正上方和正下方。

## V1.0 — 多字符数学下标分组修复（2026-08-14）

### 问题表现

- `X_out`、`X_target` 等写法只会把下划线后的第一个字符放入下标，后续字符仍留在基线，出现 `X` 的下标为 `o`、而 `ut` 悬在右侧的错误排版。
- 同类写法零散存在于任务说明、输入符号和设计方程中，单点修改无法避免再次出现。

### Bug 原因

- LaTeX 的下划线 `_` 默认只作用于紧随其后的一个 token。多字符下标未使用花括号成组，也未区分变量字母与说明性正体文本。
- 普通界面文案曾直接沿用 LaTeX 风格的纯文本名称，导致未经过数学渲染时仍显示下划线。

### 解决方式

- 全盘核查页面与计算方程中的多字符下标，统一采用成组形式，例如 `X_{\mathrm{out}}`、`X_{\mathrm{target}}`、`C_{A0}`、`F_{A0}`、`k_{\mathrm{ref}}`。
- 纯说明文字改用中文语义名称（如“出口转化率”），不再混用未渲染的 `X_out`。
- 新增源码回归规则，禁止 `X_out`、`X_in`、`X_target` 这类未成组形式重新进入可渲染公式。

### 验证

- 公式渲染测试检查多字符下标均位于同一个 KaTeX 下标组中；本地页面继续进行输入页、任务卡和结果依据的目视核查。

## V1.1 — 组分数量输入的退格过渡态修复（2026-08-17）

### 问题表现

- “反应物数量”和“产物数量”默认值为 1。用户按 Backspace 清空后，输入框会立即恢复为 1。
- 用户本想先清空再输入 2、3 或 4，但新数字会与被恢复的 1 拼成 12、13 或 14，随后又被数量上限裁剪为 4。

### Bug 原因

- 数字输入事件直接执行 `Number(event.target.value)`；JavaScript 会把空字符串转换为 0。
- 数量更新函数随即把 0 裁剪到最小值 1，受控输入框因而在用户尚未完成编辑时就重新写回 1。
- 原实现没有区分“用户正在编辑的空字符串”和“已经提交的数值状态”。

### 解决方式

- 数量字段增加只存在于输入组件内部的空字符串过渡态；Backspace 后暂不写入计算参数。
- 用户输入下一位有效数字时才提交并执行 1–4 的整数边界校验。
- 若输入框保持为空并失去焦点，则恢复最近一次有效值；初始默认值仍为 1。
- 该行为同时应用于反应物数量和产物数量，删除两处重复的即时空值数值化路径。

### 验证

- 新增源码回归测试，确保两个数量字段允许空值过渡且不再直接把空字符串传入 `Number(...)`。
- 本地浏览器分别验证“1 → Backspace → 2”不会形成 12，也不会被错误裁剪为 4。

## V1.2 — 温度分布纵轴与环境温度缺失修复（2026-08-17）

### 问题表现

- 稳态温度通常在数百 K 附近变化，但图表纵轴固定从 0 K 开始，实际温差被压缩在图顶端，趋势不易观察。
- 能量衡算使用了外界温度 `T_{env}`，结果图却只画反应物流温度，无法直观看出换热推动力的方向和大小。

### Bug 原因

- 通用折线图把所有物理量统一设为 `Y_min=0`，没有提供按变量性质选择局部纵轴范围的能力。
- 温度结果只把状态点中的 T 传入图表，输入参数中的恒定 `T_{env}` 没有形成对照序列。

### 解决方式

- 折线图增加可选的非零基线模式：按数据最小值、最大值和安全边距生成纵轴，恒定序列也保留可读范围。
- 温度图启用局部纵轴，并增加恒定环境温度曲线；当前模型明确标注为“恒定外界温度边界”。
- 增加回归测试，防止温度图重新使用零基线或遗漏环境温度。

### 验证

- 自动化测试检查温度图传入 `includeZero={false}`，且包含 `T_{env}` 序列。
- 本地浏览器复核纵轴不再从 0 K 开始，两条温度曲线及图例均正常显示。

## V1.5 — 动态转化率标签重复结果卡修复（2026-08-19）

### 问题表现

- 转化率观察组分开放选择后，由已知尺度计算时主结果名称会变为“出口 B 转化率”等动态文字。
- 旧判断只识别固定文字“出口转化率”，因此可能在主结果旁再次生成一张内容相同的出口转化率卡。

### Bug 原因

- 结果卡是否补充显示出口转化率，原先依赖完整字符串相等判断。
- V1.5 将观察组分符号加入标题后，业务含义没有改变，但字符串已不再完全相等。

### 解决方式

- 改为判断主结果是否已经以“出口”开头；已有出口转化率主卡时不再补充重复卡。
- 标签、出口状态和净消耗速率统一读取当前观察组分，删除残留的固定 A 文案。

### 验证

- 53 项前端/模型测试通过；生产构建通过。
- 浏览器自动化控制因本机 Browser 插件受信路径配置错误未能附着，本次未将该环境问题误记为页面缺陷。

## V1.6 — 旧案例基准流量迁移修复（2026-08-20）

### 问题

旧单反应案例把 `fa0` 定义为 A 的入口摩尔流率；新统一反应对象把它定义为所选转化率基准物种 L 的入口摩尔流率。若限制性反应物不是 A，直接复制数值会改变入口体积流量和设备设计尺度。

### 原因

字段名称相同，但旧、新案例的数据语义不同；仅复制扁平字段无法保持 `v0` 不变。

### 解决方式

- 迁移时按 `F_L0=F_A0(C_L0/C_A0)`（气相使用对应分压比例）换算基准流量。
- 新增统一案例迁移测试，核对计量向量、惰性组分和反应对象。
- 新界面统一显示当前转化率基准，避免把任意基准流量误写为 `F_A0`。
