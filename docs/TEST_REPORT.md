# 自动化测试报告

测试范围：

- 一级 BR 与解析式 `t=ln[1/(1−X)]/k` 比较。
- 一级 CSTR 与 `X=Da/(1+Da)` 比较。
- 一级 PFR 与 `X=1−exp(−Da)` 比较。
- Arrhenius 参考温度恒等检查。
- 气相计量关系与 PBR 压力失效边界检查。
- 通用 V1 求解接口与一级 PFR 解析解一致性检查。
- V1.1 自动生成 `2A+B→2C`、多反应物/产物/惰性物质计量与出口组成检查。
- 由 `C_{j0}/s_j` 自动判定单一或共同限制反应物，并检查等摩尔进料说明。
- 基元反应级数默认值、计量系数小写符号和计量系数—级数联动检查。
- 反应物/产物数量输入的退格空值过渡态检查。
- CSTR 转化率—体积曲线满足设计方程且不使用端点线性插值的检查。
- 由计量数和全部入口组成自动计算气相 `ε`，并检查以限制反应物为基准的出口浓度。
- 气相入口、动力学和出口状态统一采用分压基准的检查。
- 非零反应热容变化、Kirchhoff 反应焓温度修正和绝热 PFR 温升检查。
- 换热项改变 PFR 温度轨迹，以及气相 PBR 同时耦合分压动力学、温度和压降的检查。
- 可逆反应箭头、反应商、参考平衡常数、含 `ΔC_P` 的 van't Hoff 修正和平衡转化率边界检查。
- 非恒温放热可逆 PFR 中随温度移动的平衡转化率，以及逐组分摩尔流率状态点检查。
- 温度图非零纵轴、环境温度曲线、PFR/PBR 床层体积横轴和分布图可见性检查。
- 所有转化率图固定 0–1 纵轴、可逆反应 `T₀±100 K` 平衡转化率曲线和实际计算点点击提示检查。
- V1.3 平行/串联网络反应式、计量矩阵净速率、物料守恒、PFR/CSTR 一级解析解、B/C 选择性、B 收率和串联中间产物峰值检查。
- V1.4 四物种三反应自定义网络、动态结果维数、逐反应可逆净逆向、重复/不完整计量行校验及 V1.3 缓存迁移检查。
- V1.5 非 A 转化率基准解析解、分子式括号解析、元素/电荷守恒正反例、隐式快慢反应网络及 V1.4 缓存迁移检查。
- V1.6 统一液/气相物流换算、分区就绪校验、组分依赖识别和版本化案例文件往返检查。
- KaTeX 命令、字段下标、多字符下标分组、积分上下限和结果卡布局回归检查。
- 标准 Vite 本地前端构建与 TypeScript 类型检查。

验收要求：BR、CSTR、PFR 解析解相对误差不超过 `1×10⁻⁶`。测试代码以 12 位小数比较，严格于验收要求。

执行命令：

```bash
python -m unittest discover -s backend/tests -v
npm test
npm run build
```

V1.6 新增旧单反应迁移、逐反应参考温度、弹窗工作流、FEED/PRODUCT 统一物流、案例就绪状态与 JSON 案例文件检查。当前执行结果：59 项前端/模型测试与 6 项 Python 基线测试全部通过；原 V1.0–V1.5 单反应、可逆、非恒温、压降、图表与通用网络基线继续作为回归测试保留。

浏览器验收覆盖从首页直接进入完整案例、案例文件工具栏、五张摘要卡、组分依赖禁删、动态添加反应、取消草稿不写入以及 390×844 移动端断点；页面控制台无警告或错误，移动端无横向溢出。
